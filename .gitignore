# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Built search artifacts (generated by build-index script)
public/search-index.json
public/docs.json

# Graph relations — large (3+ MB) and not directly reviewable; rebuilt
# deterministically from docs.json on every CI run.
public/graph.json



# Intermediate handoff file from build:index → build:addresses
public/addresses.merged.json

# Vector index — built by build-rag (Workers AI bge-base-en).
# ~30MB binary; rebuilt fresh on every Sync DBs CI run.
public/atlas-vectors.bin
public/atlas-vectors.ids.json
public/atlas-vectors.meta.json

# Pre-Workers-AI local Ollama cache — kept ignored in case anyone re-enables
# the old offline workflow.
.cache/atlas-rag/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
note.md

# Lighthouse reports
.lighthouse-report*

.cache/pr-check/*
.cache/pr-check/pr227-ffe21c9.md

# Personal docs (not checked in)
docs/ds/
